{
  "name": "@mulholland/svengali",
  "version": "0.2.0",
  "description": "Cloudflare Worker that serves the marketing site live from GitHub, expands <include> partials, and lets logged-in editors right-click to edit text/links/images. Edits commit via the GitHub Git Data API.",
  "type": "module",
  "license": "MIT",
  "main": "./src/app.js",
  "scripts": {
    "dev": "wrangler dev --local --persist-to .wrangler/state",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "wrangler": "^4.84.0"
  }
}
